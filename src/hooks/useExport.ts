import { FormState } from '../types';

export const useExport = (formData: FormState, selectedBodyParts: string[], images: File[], showNotification: (message: string, type: 'success' | 'error' | 'info') => void) => {
  // TODO: Implement export logic if needed
  return {};
};